management:
  endpoints:
    web:
      exposure:
        include: env, refresh, health, httptrace, beans, httpexchanges, busrefresh, info, metrics, prometheus

greeting:
  message: Welcome to the REAL World.
